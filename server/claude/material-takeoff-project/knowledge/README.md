# Knowledge / reference files

Drop your reference files here (e.g. specs, unit conventions, trade lists). They will be loaded at runtime and included in the system prompt so Claude has the same context as your custom project.

Supported: any text-based files (e.g. .txt, .md). Files are read in alphabetical order.
